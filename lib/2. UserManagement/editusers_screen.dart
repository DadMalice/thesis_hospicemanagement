import 'package:flutter/material.dart';

class EditUsersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Edit Users Screen Content'),
      ),
    );
  }
}
